import { RemoveParenthesisTextPipe } from './remove-parenthesis-text.pipe';

describe('RemoveParenthesisTextPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveParenthesisTextPipe();
    expect(pipe).toBeTruthy();
  });
});
